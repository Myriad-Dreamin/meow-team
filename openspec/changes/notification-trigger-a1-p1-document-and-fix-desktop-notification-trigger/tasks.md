## 1. Proposal Alignment

- [x] 1.1 Review the approved OpenSpec artifacts for "Document and Fix Desktop Notification Triggers" and confirm the canonical request/PR title is `fix(desktop/notifications): Document and Fix Desktop Notification Triggers`
- [x] 1.2 Confirm the proposal is ready for pooled execution, the reusable worktree from `/home/kamiyoru/work/ts/meow-team/.meow-team-worktrees/meow-N` can be claimed, and conventional-title metadata `fix(desktop/notifications)` stays separate from `branchPrefix` and change paths

## 2. Implementation

- [x] 2.1 Implement the approved objective: Create `docs/notification.md`, document the exact desktop alert trigger rules and prerequisites, reproduce why notifications are not firing, and repair the client notification flow with regression coverage.
- [x] 2.2 Run validation and capture reviewer findings for "Document and Fix Desktop Notification Triggers"
