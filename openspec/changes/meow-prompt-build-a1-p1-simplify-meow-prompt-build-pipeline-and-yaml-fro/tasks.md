## 1. Proposal Alignment

- [ ] 1.1 Review the approved OpenSpec artifacts for "Simplify meow-prompt build pipeline and YAML frontmatter"
- [ ] 1.2 Confirm the proposal is ready for pooled execution and a reusable worktree from `/home/kamiyoru/work/ts/meow-team/.meow-team-worktrees/meow-N` can be claimed

## 2. Implementation

- [ ] 2.1 Implement the approved objective: Remove webpack-only support and the standalone template-sync CLI, generate prompt declaration files from the Vite lifecycle, replace custom frontmatter parsing with `stripYamlFrontmatter` plus `js-yaml`, and update configs/tests so typed prompt imports still work across Next, Vitest, and TypeScript.
- [ ] 2.2 Run validation and capture reviewer findings for "Simplify meow-prompt build pipeline and YAML frontmatter"
