## 1. Proposal Alignment

- [x] 1.1 Review the approved OpenSpec artifacts for "Support Mutable Request Titles with LLM Fallback"
- [x] 1.2 Confirm the proposal is ready for pooled execution and a reusable worktree from `/home/kamiyoru/work/ts/meow-team/.meow-team-worktrees/meow-N` can be claimed

## 2. Implementation

- [x] 2.1 Implement the approved objective: Implement an end-to-end request title field for the harness: accept an optional human title, generate a concise fallback title through the existing LLM execution path when absent, persist it as mutable request-group metadata separate from all canonical IDs, and display it in the active thread UI without removing the underlying request text.
- [ ] 2.2 Run validation and capture reviewer findings for "Support Mutable Request Titles with LLM Fallback"
