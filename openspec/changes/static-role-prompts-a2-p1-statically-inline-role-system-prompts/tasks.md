## 1. Proposal Alignment

- [x] 1.1 Review the approved OpenSpec artifacts for "Statically inline role system prompts" and confirm the canonical request/PR title is `refactor(team/prompts): Statically inline role system prompts`
- [x] 1.2 Confirm the proposal is ready for pooled execution, the reusable worktree from `/home/kamiyoru/work/ts/meow-team/.meow-team-worktrees/meow-N` can be claimed, and conventional-title metadata `refactor(team/prompts)` stays separate from `branchPrefix` and change paths

## 2. Implementation

- [x] 2.1 Implement the approved objective: Reapply the completed static `prompts/roles` registry refactor onto the current base branch, resolve merge conflicts across prompt loading, docs, and tooling, and rerun validation so runtime filesystem reads stay removed and the change is merge-ready.
- [x] 2.2 Run validation and capture reviewer findings for "Statically inline role system prompts"
